# Python 3.10+ recommended

# Core scientific stack
numpy>=1.24,<3
pandas>=2.0,<3
scipy>=1.11,<2
scikit-learn>=1.3,<2
matplotlib>=3.7,<4

# App framework
streamlit>=1.34,<2

# PDF/reporting
reportlab>=4,<5
pillow>=10,<12  # for PNG/JPEG handling in ReportLab ImageReader

# Optional: faster CSV/Parquet (safe to keep; Streamlit Cloud has wheels)
pyarrow>=14,<19
